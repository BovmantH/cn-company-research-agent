# =============================================================================
# cn-company-research — 环境变量样例
# -----------------------------------------------------------------------------
# 本文件**仅作为模板**,实际运行请复制为 `.env` 后再填写敏感值。
# 本项目所有 LLM 调用统一走 backend/services/llm_factory.get_llm,
# 检索调用统一走 backend/services/search.get_search_provider。
# =============================================================================


# -----------------------------------------------------------------------------
# 1) LLM 凭证(必填:下方任意一个 vendor key 至少配一个,否则启动会被拒绝)
# -----------------------------------------------------------------------------
# 工厂在启动期按"vendor 优先级"探测,**单 vendor 全包**:
# 命中的第一家 vendor 接管所有 role(researcher / briefing / editor)。
# 默认探测顺序:DeepSeek → Qwen → Kimi → OpenRouter → OpenAI
# 想换顺序见 §2 的 LLM_VENDOR_PRIORITY;想锁死一家见 LLM_VENDOR。


# 1.1) 国产原厂直连(推荐,无需代理)
# -----------------------------------------------------------------------------

# DeepSeek 原厂  https://api-docs.deepseek.com/
#   端点: https://api.deepseek.com/v1
#   默认 slug: deepseek-v4-flash(non-thinking)
#   ⚠ 老 slug deepseek-chat / deepseek-reasoner 将于 2026/07/24 退役,
#     当前自动路由到 deepseek-v4-flash / deepseek-v4-pro,新配置请直接用 V4 slug
DEEPSEEK_API_KEY=

# 阿里百炼(Qwen 兼容模式)
# 文档: https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope
#   端点: https://dashscope.aliyuncs.com/compatible-mode/v1(北京);
#         国际站走 https://dashscope-intl.aliyuncs.com/compatible-mode/v1
#   默认 slug: researcher → qwen3.6-flash,briefing → qwen3.6-plus,editor → qwen3-max
DASHSCOPE_API_KEY=

# Moonshot(Kimi)  https://platform.moonshot.cn/
#   端点: https://api.moonshot.cn/v1
#   默认 slug: researcher/editor → kimi-k2.5(多模态旗舰),briefing → kimi-k2-turbo-preview
MOONSHOT_API_KEY=

# 小米 MiMo  https://api.xiaomimimo.com/
#   端点: https://api.xiaomimimo.com/v1
#   默认 slug: mimo-v2.5-pro(三个 role 都用)
#   另有 token plan 充值版端点 https://token-plan-cn.xiaomimimo.com/v1,
#   通过 LLM_BASE_URL_MIMO 切换。
XIAOMI_API_KEY=


# 1.2) 聚合 / 海外 vendor(国内通常需要代理)
# -----------------------------------------------------------------------------

# OpenRouter  https://openrouter.ai/
#   一个 key 通几百种模型,model slug 必须带 vendor/ 前缀(如 deepseek/deepseek-v4-flash)
OPENROUTER_API_KEY=

# OpenAI 原生  https://platform.openai.com/
OPENAI_API_KEY=


# -----------------------------------------------------------------------------
# 2) Vendor 选择策略(可选)
# -----------------------------------------------------------------------------

# 显式锁定 vendor,跳过探测。值取自:deepseek / qwen / kimi / mimo / openrouter / openai
# 锁定后对应 key 缺失会直接报错,不退回探测(避免账单 / 合规意外)。
# LLM_VENDOR=deepseek

# 自定义优先级,逗号分隔。未设置时用默认: deepseek,qwen,kimi,mimo,openrouter,openai
# 未列出的 vendor **不会被探测**;含未知名字会 WARN 并跳过。
# 例:更偏好 Qwen,然后 OpenRouter 兜底
# LLM_VENDOR_PRIORITY=qwen,openrouter


# -----------------------------------------------------------------------------
# 3) 各角色模型选择(可选,不填走代码内默认)
# -----------------------------------------------------------------------------
# - vendor=openrouter 时:slug 必须带 vendor/ 前缀(原样发给 OpenRouter)
# - vendor=原厂/OpenAI 时:slug 不带前缀,例如 deepseek-v4-flash、qwen3-max、kimi-k2.5、gpt-5.5
#   如果误填了带前缀的 OpenRouter slug,工厂会 WARN 并自动剥离前缀

# LLM_MODEL_RESEARCHER=
# LLM_MODEL_BRIEFING=
# LLM_MODEL_EDITOR=


# -----------------------------------------------------------------------------
# 3.1) OpenRouter slug 速查表(2026-05,仅 vendor=openrouter 时生效)
# -----------------------------------------------------------------------------
# 走原厂直连请用各 vendor 自己的 slug(见 1.1 段落注释)。
#
# DeepSeek:
#   deepseek/deepseek-v4-flash        通用对话,便宜量大
#   deepseek/deepseek-v4-pro          旗舰
#
# 通义千问 (Alibaba Qwen):
#   qwen/qwen3.6-flash                长上下文,稳定
#   qwen/qwen3.6-max-preview          旗舰预览
#
# Moonshot Kimi:
#   moonshotai/kimi-k2.6              262K 上下文,代码强
#   moonshotai/kimi-k2-thinking       agentic 长链路推理
#
# === 推荐组合 ===
#
# A) 全国产 · OpenRouter(只有一个 OpenRouter key 时用):
#   LLM_MODEL_RESEARCHER=deepseek/deepseek-v4-flash
#   LLM_MODEL_BRIEFING=qwen/qwen3.6-flash
#   LLM_MODEL_EDITOR=moonshotai/kimi-k2.6
#
# B) 全国产 · OpenRouter · 高质量:
#   LLM_MODEL_RESEARCHER=deepseek/deepseek-v4-pro
#   LLM_MODEL_BRIEFING=qwen/qwen3.6-max-preview
#   LLM_MODEL_EDITOR=moonshotai/kimi-k2-thinking
#
# C) 原厂直连 · DeepSeek 全包(只配 DEEPSEEK_API_KEY,模型字段全空走默认):
#   # 不需要写任何 LLM_MODEL_*,默认 deepseek-v4-flash 即可
#
# D) 原厂直连 · 多家共存按优先级:
#   # 配上 DEEPSEEK_API_KEY + DASHSCOPE_API_KEY,默认会用 DeepSeek
#   # 想优先 Qwen 则 LLM_VENDOR_PRIORITY=qwen,deepseek


# -----------------------------------------------------------------------------
# 4) LLM 通用参数(可选)
# -----------------------------------------------------------------------------

# 采样温度,默认 0(确定性输出,便于复现)
# LLM_TEMPERATURE=0

# 是否启用流式输出,默认 true(briefing 节点会显式覆盖为 false 以减少连接开销)
# LLM_STREAMING=true

# 全局 base_url 覆盖,优先级最高(所有 vendor 都被替换为此值)
# 用途:走本地 vLLM / Ollama / LiteLLM,或自建 OpenAI 兼容代理
# 示例: LLM_BASE_URL=http://localhost:8000/v1
# LLM_BASE_URL=

# 单 vendor 维度的 base_url 覆盖,仅在该 vendor 被选中时生效。
# 优先级低于全局 LLM_BASE_URL,高于 vendor 默认 base_url。
# 用途:用 OneAPI / LiteLLM 网关只代理某一家原厂。
# LLM_BASE_URL_DEEPSEEK=
# LLM_BASE_URL_QWEN=
# LLM_BASE_URL_KIMI=
# LLM_BASE_URL_MIMO=        # 切到 token plan: https://token-plan-cn.xiaomimimo.com/v1
# LLM_BASE_URL_OPENROUTER=
# LLM_BASE_URL_OPENAI=

# 单次响应 max_tokens 上限(强烈推荐配置)
# OpenRouter 按 max_tokens × 单价预扣费,不设的话会按模型最大窗口
# (数万 token)预扣,贵模型 + 小余额会直接 402 报错。
# 推荐值:4096(够用),报告类长输出可调到 8192。
# LLM_MAX_TOKENS=4096


# -----------------------------------------------------------------------------
# 5) 检索 provider(必填 TAVILY_API_KEY)
# -----------------------------------------------------------------------------

# Tavily API key,Phase 1 默认 provider 必填
# 申请地址: https://tavily.com/
TAVILY_API_KEY=

# 切换检索 provider,默认 `tavily`
# Phase 2 计划新增 `bocha` / `serper` / `zhipu` 等中文友好 provider
# SEARCH_PROVIDER=tavily


# -----------------------------------------------------------------------------
# 6) MongoDB 持久化(可选)
# -----------------------------------------------------------------------------
# 不配置则结果只保留在内存,重启即丢;配置则启用任务/报告持久化。
# 示例: MONGODB_URI=mongodb://localhost:27017
# MONGODB_URI=


# -----------------------------------------------------------------------------
# 7) 网络代理(可选,仅在国内访问 OpenRouter / OpenAI / Tavily 受限时需要)
# -----------------------------------------------------------------------------
# 走原厂直连(DeepSeek / Qwen / Kimi)无需代理。
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# 允许访问后端的前端来源，多个来源使用英文逗号分隔。
# 默认值与仓库内 Vite、Docker Compose 的 5174 端口一致。
# ALLOWED_ORIGINS=http://localhost:5174

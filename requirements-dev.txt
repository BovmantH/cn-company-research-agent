# 开发依赖（测试 + lint）
# 安装：uv pip install --python .venv/Scripts/python.exe -r requirements-dev.txt
# 验证：ruff format --check . && ruff check . && pytest

pytest==8.3.4
pytest-asyncio==1.4.0
pytest-mock==3.14.0
mongomock==4.3.0
ruff==0.16.2
